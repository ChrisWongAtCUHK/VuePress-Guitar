{
  "name": "vuepress-guitar",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "deploy": "yarn build; gh-pages -d dist",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "export": "vuepress export docs"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-vuepress": "^2.1.0",
    "gh-pages": "^2.2.0",
    "husky": "^4.2.3",
    "lint-staged": "^10.0.8",
    "vuepress": "^1.3.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "git add"
    ]
  }
}
